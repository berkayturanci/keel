⚡ Bolt: Combine regex markers to optimize text matching

💡 What: Combined multiple regular expressions for `rate_limited` and `vendor_timed_out` checks into single compiled patterns using the `|` (OR) operator. Replaced iterative `any()` calls with a single `.search()` call.

🎯 Why: To eliminate the overhead of Python generator expressions and repeated C-level switches when evaluating multiple independent regular expressions.

📊 Impact: Execution time for `rate_limited` improves by ~80% (from ~3.06µs to ~0.58µs) and for `vendor_timed_out` by ~42% (from ~2.68µs to ~1.54µs).

🔬 Measurement: Evaluated with Python's `timeit` using 1,000,000 iterations against matching strings `status: 429` and `the request timed out after a while`.

agent:bolt

Closes #no issue
