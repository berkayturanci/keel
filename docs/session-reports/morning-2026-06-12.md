# keel — Morning Brief · 2026-06-12

_Generated 2026-06-12 10:55 Europe/Istanbul · since 2026-06-11T07:55Z (first run; default 24h window)_

GitHub via MCP transport. keel-CLI native `gh`/`gh-auth` degraded → health `ci` provider `_unavailable_` through keel; CI status below read directly from Actions.

## Overnight deferrals
None — deferral store unconfigured, run ledger empty (`capture_health: clean`, 0 records).

## Shipped (last 24h)
**22 PRs merged to `main`** (no closed-without-merge):
- **fix ×12** — redaction credential-leak hardening (#272, #286, #294), evidence fail-closed on untrusted/empty authors (#276, #277, #270), jury oversize-diff gating (#273, #287), CI skipped-rollup semantics (#288), scaffold YAML quoting (#296), CLI state-path reporting (#278), install legacy-wrapper noise (#274).
- **feat ×3** — fail-closed core merge command (#269), step verification + run controls (#271), deterministic post-comment helper (#275).
- **ci ×3** — release wheel install w/o dep resolution (#298), hash-lock publish tooling (#290), quote ship workflow args (#289).
- **docs ×4** — per-flag CLI/ship-grammar reference (#303), 2026-06-11 security audit report (#301), ship.md s10 ↔ `keel merge` alignment + 1.2.1 changelog (#297), unreleased hardening notes (#279).
- plus chore audit follow-ups (#255).

Theme: a security-hardening sprint (redaction + evidence + jury fail-closed) landing alongside the new core `keel merge` / step-verification machinery.

## Capture Health
`clean` — 0 ledger records, no missing markers, no allowed skips, no deferred capture, no reconcile actions. (Dry-run summary; no mutations made.)

## Window status
🟢 **OPEN** — Europe/Istanbul, merge_window 07:00–01:30 (now ~10:55).

## Suggested Focus
1. **PR #304 — Bolt perf optimization** (`intake.py`/`closure.py` set/`endswith` hot-paths). Core CI + CodeQL green, but `keel-ship` automation workflow **failing** and no review yet. Triage the keel-ship failure, review, then merge-or-close. _(touches `intake.py`/`closure.py` — not tier-3, low risk.)_
2. **PR #305 — Palette a11y** (website `<pre>` roles / decorative-icon `aria-hidden`). Same state: CI + CodeQL green, `keel-ship` workflow failing, unreviewed. Docs/static, low risk — quick win once keel-ship is explained.
3. **Issue #299 — website flagship vs. backbone restructure** (`documentation`). Only open issue; aligns `website/index.html` with the engine-vs-consumer framing. Static-site, low risk — good backlog pickup once #305 clears the same file area.

_No production fires (no health/telemetry provider configured; `main` CI green). No stale PRs (both open PRs opened 2026-06-11, <3d). No unassigned bugs. No CI failures on `main`._

_No `priorities.md` present → no manual-focus note._
